# ── xLights shows (under "shows/") ────────────────────
# Rendered output (regenerable from .xsq)
shows/**/*.fseq

# xLights auto-backups (git is our versioning now)
shows/**/Backup/
shows/**/*.xbkp
*.jbkp

# Media (large binaries)
shows/**/*.wav
shows/**/*.mp3
shows/**/*.mp4
shows/**/*.avi

# ── Downloaded tools / OS images (re-downloadable, huge) ─
*.img
*.img.zip
*.dmg

# ── OS cruft (anywhere in repo) ───────────────────────
.DS_Store
**/.DS_Store
